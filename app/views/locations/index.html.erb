<!DOCTYPE html>
<html>
	<head>
		<title>Trip Planner</title>
	</head>
	<body>
    <ul class="navbar">
      <a href="index.html">Home</a>
      <a href="show.html">My Trips</a>
      <a href="about.html">About us</a>
      <%= button_to "Log Out", destroy_user_session_path, method: :delete %>
    </ul>
  <header class='masthead'>
    <p class='masthead-intro'>My Trip</p>
    <h1 class='masthead-heading'>PLANNER</h1>
  </header>
  <section>
   <h2>Welcome <%= current_user.first_name %></h2>
   <hr />
   <%= link_to "Add Location", new_account_path %>
 <h3>Your Account</h3>
 <ul>
   <% @accounts.each do |account| %>
     <li>
       <%= a.name %> - $<%= a.balance %>
     </li>
   <% end %>
 </ul>
  </section>

  <footer class="content-footer">
      <p>Wanderlust!</p>
  </footer>
</body>
</html>